<?php

namespace Alterio\GoogleReviews;

use Google\Client;
use Google\Service\MyBusinessBusinessInformation;

class GoogleReviews
{
    public function __invoke()
    {
        // $client = new Client();
        // $client->setAuthConfig(dirname(__DIR__) . '/oauth.json');
        // $client->addScope('https://www.googleapis.com/auth/business.manage');

        // $business = new MyBusinessBusinessInformation($client);
        // $locations = $business->accounts_locations;
        // $queryParams = [
        //     "pageSize" => 10,
        // ];
    }
}
